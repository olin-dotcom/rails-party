<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @factory.name %>
</p>

<p>
  <b>Location:</b>
  <%= @factory.location %>
</p>

<p>
	<b>Widgets:</b>
	<ul>
	<% @factory_widgets.each do |widget| %>
		<li><%= link_to widget.name, widget %></li>
	<% end %>
	</ul>
<p>
	<%= render 'widgets/form' %>
</p>


<%= link_to 'Edit', edit_factory_path(@factory) %> |
<%= link_to 'Back', factories_path %>
